# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to Git!

# Database Configuration
DB_HOST=localhost
DB_USER=your_database_user
DB_PASSWORD=your_secure_password_here
DB_NAME=your_database_name

# Server Configuration
PORT=3001
NODE_ENV=development

# Session Security
SESSION_SECRET=generate_a_very_long_random_string_here_at_least_32_characters

# JWT Security (if implementing JWT)
JWT_SECRET=another_very_long_random_string_for_jwt_tokens
JWT_EXPIRE=24h

# Email Configuration (if implementing email features)
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_app_password
EMAIL_SERVICE=gmail

# API Keys (replace with actual services you use)
# EXAMPLE_API_KEY=your_api_key_here
# ANOTHER_SERVICE_KEY=another_key_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Security Notes:
# 1. Generate strong, random secrets using: openssl rand -base64 32
# 2. Use different values for development, staging, and production
# 3. Rotate secrets regularly
# 4. Never share credentials in plain text
# 5. Use environment-specific configurations